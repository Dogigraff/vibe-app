🔍 SEO-аудит VIBE — Оценка Google
Тип проекта: Next.js + Supabase + Telegram WebApp
Дата аудита: 21 февраля 2026

🏆 Итоговая оценка
Категория	Оценка	Балл
Метаданные (title, description, OG)	🔴 Критично	2/10
Индексирование (robots, sitemap)	🔴 Критично	1/10
Технический SEO (h1, семантика)	🟡 Слабо	4/10
Производительность (Core Web Vitals)	🟡 Слабо	5/10
Контент и ключевые слова	🔴 Критично	2/10
Мобильная оптимизация	🟢 Хорошо	7/10
Структурированные данные (JSON-LD)	🔴 Критично	0/10
Безопасность (HTTPS, CSP)	🟢 Хорошо	7/10
Доступность (Accessibility)	🟡 Слабо	4/10
Технический стек	🟢 Отлично	9/10
🎯 Общий балл: 4.1 / 10
⚠️ Главный контекст
IMPORTANT

VIBE — это Telegram Mini App. Пользователи заходят через Telegram, а не через Google. Классическое SEO здесь частично нерелевантно, однако:

У приложения есть публичная веб-страница (/ — лендинг)
Прямые ссылки на сайт могут индексироваться Google
Для веб-версии SEO критично важно для органического трафика
🔴 Критические проблемы
1. Нет robots.txt
❌ /robots.txt — файл отсутствует
Google-бот не знает, что индексировать, а что нет. Закрытые страницы (/map, /profile) могут попасть в индекс.

Решение: создать app/robots.ts:

ts
export default function robots() {
  return {
    rules: { userAgent: '*', allow: '/', disallow: ['/map', '/profile', '/login', '/api/'] },
    sitemap: 'https://yoursite.com/sitemap.xml',
  }
}
2. Нет sitemap.xml
❌ /sitemap.xml — файл отсутствует
Google не видит структуру сайта. Индексирование случайное.

Решение: создать app/sitemap.ts:

ts
export default function sitemap() {
  return [{ url: 'https://yoursite.com', lastModified: new Date() }]
}
3. Слабые метаданные в 
layout.tsx
ts
// Текущее (очень мало):
export const metadata: Metadata = {
  title: "VIBE - Find your vibe in 5 minutes",
  description: "Real-time geo-social network",
};
Отсутствуют: Open Graph, Twitter Card, keywords, canonical URL, favicon, viewport, theme-color.

Решение:

ts
export const metadata: Metadata = {
  title: "VIBE — Find Your Vibe in 5 Minutes",
  description: "Real-time geo-social network. Meet people around you in 5 minutes via Telegram.",
  keywords: ["geo social", "meet people", "telegram app", "vibe"],
  openGraph: {
    title: "VIBE — Find Your Vibe",
    description: "Meet people around you in 5 minutes.",
    url: "https://yoursite.com",
    siteName: "VIBE",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "VIBE",
    description: "Find your vibe in 5 minutes.",
    images: ["/og-image.png"],
  },
  metadataBase: new URL("https://yoursite.com"),
  robots: { index: true, follow: true },
};
4. Нет OG-изображения (og-image.png)
Когда кто-то делится ссылкой в соцсетях или Telegram — карточка пустая. Это убивает CTR.

Решение: создать app/opengraph-image.tsx (Next.js автогенерация) или добавить статичный /public/og-image.png (1200×630px).

5. Нет иконки (favicon)
В public/ нет favicon.ico или icon.png. В браузере показывается стандартный значок Next.js.

Решение: добавить app/icon.png (512×512px) — Next.js 13+ подхватит автоматически.

6. Нет структурированных данных (JSON-LD)
❌ Schema.org разметка — полностью отсутствует
Google не может понять: это приложение? Сервис? Организация?

Решение: добавить в 
layout.tsx
:

tsx
<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify({
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "VIBE",
  "description": "Real-time geo-social network",
  "applicationCategory": "SocialNetworkingApplication",
  "url": "https://yoursite.com"
}) }} />
🟡 Средние проблемы
7. lang="ru" при англоязычном контенте
html
<html lang="ru">  <!-- ❌ Неверно -->
Лендинг написан на английском (Don't be lonely. Find your vibe in 5 minutes.), а lang указан как ru. Google-переводчик и скринридеры путаются.

Решение: сделать lang="en" или добавить hreflang для мультиязычности.

8. Лендинг-страница — это "use client" с мгновенным редиректом
tsx
// В LandingOrLogin.tsx:
useEffect(() => {
  if (isTelegramWebApp()) router.replace("/login");  // JS-редирект
}, [router]);
Если Google-бот зайдёт на / — он увидит только JS, без реального контента. Это не SEO-friendly.

Решение: Лендинг (/) должен быть Server Component с реальным HTML-контентом (описание, заголовки, CTA). Клиентский редирект только для Telegram-среды.

9. Слабая иерархия заголовков
Лендинг: <h1>VIBE</h1> — слишком короткий
Login-страница: несколько <h1> в разных состояниях одного компонента — дублирование
Profile-страница: нет <h1> вообще
10. 
next.config.js
 слишком пустой
js
// Сейчас:
const nextConfig = { reactStrictMode: true };
Отсутствуют: заголовки безопасности (CSP, X-Frame-Options), оптимизация изображений, сжатие.

Рекомендация:

js
const nextConfig = {
  reactStrictMode: true,
  compress: true,
  images: { formats: ['image/avif', 'image/webp'] },
  async headers() {
    return [{ source: '/(.*)', headers: [
      { key: 'X-Frame-Options', value: 'DENY' },
      { key: 'X-Content-Type-Options', value: 'nosniff' },
      { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
    ]}]
  }
}
🟢 Что уже хорошо
✅	Описание
Next.js App Router	SSR из коробки — правильный выбор для SEO
HTTPS через Supabase/Vercel	Безопасное соединение
Семантический <main>	Используется на всех страницах
Адаптивный дизайн	TailwindCSS, mobile-first
TypeScript	Меньше runtime-ошибок
middleware.ts
Корректная защита маршрутов
📋 Приоритетный план исправлений
НЕДЕЛЯ 1 (Критично):
[ ] 1. Создать /public/og-image.png и /public/favicon.ico
[ ] 2. Расширить metadata в layout.tsx (OG, Twitter, canonical)
[ ] 3. Создать app/robots.ts
[ ] 4. Создать app/sitemap.ts
[ ] 5. Исправить lang="ru" → lang="en"
НЕДЕЛЯ 2 (Важно):
[ ] 6. Переписать лендинг как SSR с реальным HTML-контентом
[ ] 7. Добавить JSON-LD разметку
[ ] 8. Расширить next.config.js (заголовки, сжатие)
[ ] 9. Добавить индивидуальные metadata для /login
НЕДЕЛЯ 3 (Улучшение):
[ ] 10. Добавить aria-label на интерактивные элементы
[ ] 11. Оптимизировать Core Web Vitals (LCP, CLS, FID)
[ ] 12. Настроить Google Search Console
💡 Ключевой вывод
NOTE

Проект технически построен на правильном стеке (Next.js App Router). Фундамент хороший, но SEO-слой полностью не реализован. Это типично для MVP Telegram-приложений — команда фокусировалась на функционале, а не на органическом поиске. Все проблемы решаемы за 1-2 недели без изменения архитектуры.